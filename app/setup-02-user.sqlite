CREATE TABLE IF NOT EXISTS user (
    id                  INTEGER PRIMARY KEY
,   login               VARCHAR(160)
,   password            VARCHAR(160)
,   level               INT
,   creationDate        DATETIME
);
